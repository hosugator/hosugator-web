// data/projectDetails.en.ts
// Project detail (web) content — English translation of projectDetails.ts. Keyed by slug.
// When this data exists the detail page renders rich content; otherwise only the overview shows.

import type { ProjectDetailContent } from './projectDetails';

export const projectDetailsEn: Record<string, ProjectDetailContent> = {
  'edge-ai-lmr': {
    context:
      'On DTK\'s lens thermoforming line, the PLC generates temperature, pressure, and power sensor data every 10ms — yet there was no intelligent system to analyze or prescribe on that stream in real time. When anomalies occurred, response depended solely on the experience of skilled operators, causing delays. Multi-axis sensor data could not be linked spatio-temporally by timestamp alone, so anomaly detection, quality prediction, and prescriptive control were all structurally blocked.',
    decision:
      'The goal was set beyond simple anomaly detection: to complete a full prescriptive control loop (M1 to M2 to M3). The Field / Control / Edge / Cloud layers were separated into independent deployment units, and Cycle_ID was used as the Golden Key to join spatio-temporal data across every layer on a single key. Communication was split into three tiers by data temperature (MQTT Binary HOT, gRPC Streaming WARM, Parquet COLD) to optimize throughput, latency, and cost simultaneously.',
    implementation: [
      // Correction: this used to conflate two separate models. M1 is time-series sensor
      // anomaly detection (1D-CNN AE); PatchCore belongs to the vision AOI workstream
      // (see the 'v1-aoi' entry below).
      { title: 'M1 — Anomaly Detection', body: 'An Anomaly Score based on 1D-CNN Autoencoder reconstruction error reached AUROC 99.99%. The score is passed as an input feature to M2, chaining the modules together.' },
      { title: 'M2 — Quality Prediction', body: 'An ensemble of LSTM (long-range temporal patterns) and XGBoost (nonlinear features). The M1 score is fed in jointly to raise prediction accuracy, and the prediction is passed to M3 as its State.' },
      { title: 'M3 — Prescriptive Control', body: 'A Deep Q-Network computes optimal temperature and pressure set-points as Actions and feeds them back to the PLC — a closed-loop controller, currently in simulation-validation.' },
      { title: 'Data Communication 3-Tier', body: 'HOT: MQTT QoS0 Binary (lossless 10ms buffering). WARM: gRPC bidirectional streaming (Protobuf). COLD: Parquet batch offload for drift retraining.' },
    ],
    results:
      'Achieved anomaly-detection AUROC 99.99%. A React18 + TS HMI dashboard (Process, Quality, Anomaly, Energy — four views) uses a shared Zod schema to enforce the FE-BE API contract at compile time, eliminating type mismatches. Next steps: fail-safe and SIL safety validation before deploying the M3 DQN on a real PLC, plus a VLM-based natural-language anomaly-explanation layer.',
    stack: ['MQTT', 'gRPC', 'Node.js', 'React18 + TS', 'Zod', 'PyTorch'],
  },

  'v1-aoi': {
    // Mirror of the KO entry. Keep both in sync when filling the TODOs.
    context:
      'This is the vision-inspection workstream of Edge AI LMR (the lens thermoforming Factory OS). Where the process side (M1–M3) works on PLC time series, this side handles image-based surface inspection, and ADR-020 scopes it as a standalone delivery package. ' +
      'Lens surface contamination was inspected by eye, so verdicts drifted with inspector fatigue. Solving it with supervised learning would require images labeled per defect type — but defects are rare on a real line and the full set of types is not knowable in advance, making label acquisition itself the bottleneck.',
    decision:
      'We abandoned "learn the defects to find them" and reframed the problem as "find what is not normal." PatchCore builds a memory bank from patch embeddings of normal images only, then scores anomalies at inference by distance to the nearest normal patch. No defective image is needed for training, so the labeling bottleneck disappears and the model also reacts to unseen defect types.',
    implementation: [
      { title: 'PatchCore setup', body: 'Uses intermediate features from layer2 and layer3 of a WideResNet50 backbone, with the memory bank compressed by 10% coreset sampling. Shallow layers carry texture and deeper layers carry structure, so both are needed to catch fine contamination as well as shape defects.' },
      { title: 'Data composition', body: 'Training used 267 normal images only. The test split separates 68 normal and 243 contaminated images, so false positives can be measured against a normal set never seen during training.' },
      { title: 'circle-crop preprocessing', body: 'A model trained on the full 1120×1120 frame reacted to label stickers and background texture as anomalies, making localization meaningless. Cropping the circular lens region to 236×236 removed the background variable and confined the response to the contamination itself.' },
      { title: 'ONNX inference path', body: 'export.py converts the checkpoint to ONNX so an edge PC can infer with onnxruntime alone, without a PyTorch runtime installed — chosen to reduce dependencies and image size in the delivered environment.' },
    ],
    results:
      'Image AUROC 0.9906 and F1 0.9879 on contamination verdicts. CPU-only inference runs at 254.9ms/frame (P95 279.7ms), still short of the 100ms edge target — ONNX quantization or a lighter backbone remains the open task before real-time inline inspection. Pixel-level AUROC is unmeasured: the Folder dataset has no pixel mask ground truth (mask_dir), so localization accuracy cannot be verified numerically and currently rests on qualitative heatmap review. Acquiring a subset of mask labels is the next priority. The biggest lesson was that input definition mattered more than the model — on full frames the background and label stickers registered as anomalies, making localization meaningless, but cropping to the circular lens region made the same model isolate contamination cleanly.',
    stack: ['PatchCore', 'anomalib v2', 'PyTorch', 'WideResNet50', 'ONNX Runtime', 'OpenCV'],
  },

  'alignai': {
    context:
      'The existing OpenCV filtering (Canny Edge + manual thresholds) on DTK\'s lens alignment process was fragile against changes in lighting and background. Because thresholds had to be re-tuned by hand, process automation was fundamentally impossible; even a slight change in lighting led to alignment failure, forcing a skilled operator to be on standby at all times.',
    decision:
      'Accepting the limits of the rule-based approach, I shifted the paradigm to a learning-based one (U-Net segmentation). Deep learning — robust to lighting and background changes with no thresholds — fully replaced the old pipeline. Beyond that, I integrated an ML CI/CD system with GitHub as the SSOT (GitOps), separating the lifecycles of the inference (Deployment) and training (Job) images, together with an on-site LLM agent.',
    implementation: [
      { title: 'EfficientNet-B0 Encoder', body: 'ImageNet pretraining sped up convergence 3x, and Skip Connections preserve spatial information — such as alignment-line position and orientation — directly into the decoder.' },
      { title: 'Dice Loss', body: 'More robust to class imbalance (99% background vs. 1% alignment line) than BCE. Optimal for segmenting thin lines with an extremely small pixel count.' },
      { title: 'Overfitting Prevention', body: 'Albumentations augmentation (rotation, brightness, Gaussian noise) plus validation-based Early Stopping secured generalization on a small dataset.' },
      { title: 'GitOps ML CI/CD', body: 'code push to ci.yml (pytest to build) to GHCR (SHA) to Argo CD rolling update. Training runs via workflow_dispatch to a GPU k3s Job, with fingerprint-based skipping and ONNX export. Three troubleshooting cases (GITHUB_TOKEN triggering, Argo Job re-creation, etc.) are captured as ADRs.' },
    ],
    results:
      'On field data, achieved 100% alignment-line detection, a 91% PASS rate, and ~330ms CPU inference. Manual threshold tuning was fully eliminated, enabling fully automated alignment and reducing operator standby time to zero. k3s self-healing recoverability against OOM and abnormal process termination was verified locally.',
    stack: ['PyTorch', 'U-Net', 'EfficientNet-B0', 'ONNX', 'Docker', 'k3s', 'Argo CD', 'GitHub Actions', 'React + TS'],
  },

  'erp-backup': {
    context: 'In my first week, the absence of an official API for the legacy web ERP (K-System Ace) was identified as the biggest bottleneck for data migration. Its non-standard dynamic-popup structure made existing automation tools (Selenium, AutoHotkey) unusable, and manually extracting tens of thousands of approval documents was a repetitive bottleneck eating up company-wide resources. The asynchronous open/close behavior of the popups was the root cause of every existing approach failing.',
    decision: 'I chose Playwright, because of its decisive advantage over Selenium in asynchronous event handling and dynamic-popup control. Promise.all concurrency structurally eliminated the race condition in popup-event timing, and the POM (Page Object Model) pattern secured a maintenance architecture robust against UI structure changes. A security-first design (credential separation) and a full CSV audit-trail log (auditability) were included from the planning stage.',
    implementation: [
      { title: 'Promise.all Concurrency Design', body: 'Handling the popup-event listener and the click action together with Promise.all structurally resolved the popup-timing race condition that arises with sequential processing. Parallelization also improved throughput, and failures are handled by individual rejects.' },
      { title: 'Maintainability via POM Pattern', body: 'The Page Object Model abstracts the ERP UI into LoginPage, DocumentListPage, and PopupHandler classes. When the UI structure changes, only the relevant POM class needs editing, and test code and logic can be reused.' },
      { title: 'Security-first & Auditability', body: '.env + .gitignore fully separate credentials, and a full CSV audit-trail log — recording date, document number, PDF status, attachment presence, and remarks — guarantees auditability. Failed items are automatically retried through a reprocessing pipeline.' },
      { title: 'Reprocessing & Portability Design', body: 'CSV checkpoints skip already-processed documents to support resume-after-interruption, and dynamic pagination is handled automatically. Thanks to the POM structure and .env injection, the code is reusable on other ERP systems with no modification.' },
    ],
    results: 'Converted a manual task that used to take weeks into fully unattended automation, achieving 100% data integrity. The reprocessing logic completes the full set with no omissions even on partial failure, resolving the company-wide migration bottleneck. It proved — in my first week — that the essence of automating an API-less legacy system is timing control and state tracking, following a "identify the bottleneck to design automation to apply company-wide" arc.',
    stack: ['Playwright', 'Node.js', 'Promise.all', 'POM Pattern', 'TypeScript', '.env Security', 'CSV Audit Log'],
  },
  'dotodo': {
    context: 'A RAG service that accumulates daily-life data from the user\'s voice (STT) and recommends context-appropriate to-dos via vector similarity search. There were three core challenges. (1) Cold Start — no recommendations possible when a new user has no vector data. (2) LLM API latency — synchronous processing makes the user wait for a response. (3) Recommendation-quality validation — a mechanism was needed to automatically judge the relevance of recommendations without a human in the loop.',
    decision: 'I designed an MSA that separates the Backend (user management, authentication) and the Model Server (AI inference, RAG) onto distinct AWS EC2 instances, ensuring the Backend stays up during model upgrades. Mecab-ko morphological analysis improved Korean vector-search quality, and an LLM-as-a-Judge loop provided autonomous quality validation. FastAPI asyncio minimized LLM-call latency.',
    implementation: [
      { title: 'Mecab-ko NLP Pipeline', body: 'After Korean morphological analysis and stopword removal, 768D embeddings feed a ChromaDB cosine-similarity Top-K=3 search. Cold Start injects initial values based on popular tasks to guarantee a minimum set of recommendations.' },
      { title: 'LLM-as-a-Judge Quality Loop', body: 'An autonomous quality-validation loop in which the LLM evaluates the relevance and usefulness of its own recommendations. Low-scoring recommendations trigger regeneration, maintaining quality standards without a human, while balancing the cost-vs-quality trade-off.' },
      { title: 'FastAPI asyncio Parallelization', body: 'asyncio minimizes LLM API-call latency and chains Mecab-ko normalization, vector search, and LLM calls into an asynchronous pipeline. The Model Server is redeployed independently while the Backend stays up.' },
      { title: 'LLM Judge Cost Optimization', body: 'The Judge call is applied selectively only to the lowest-scoring item among the Top-K recommendations, cutting Judge API cost by 60% versus calling on everything. Regeneration is triggered only when the quality threshold is not met.' },
    ],
    results: 'Implemented Top-K=3 personalized recommendations and, through MSA separation, achieved zero-downtime model upgrades. The asyncio pipeline cut LLM response latency by 60%, and LLM-as-a-Judge managed recommendation quality autonomously. Cold Start initial-value injection is a stopgap — similar-user clustering (User-Based CF) is the long-term solution, and optimizing LLM Judge call cost remains an open task.',
    stack: ['FastAPI', 'LangChain', 'ChromaDB', 'Mecab-ko', 'PostgreSQL', 'AWS EC2 MSA', 'STT', 'Swift(iOS)'],
  },
  'sodamdiary': {
    context: 'A diary app that narrates photos taken by visually impaired users through voice. Using GPT-4V alone incurred KRW 1.3M/month in cost and a 30-second response latency that severely degraded the UX. As a multimodal task requiring both concrete description (objects, text) and emotional atmosphere (warmth, vividness), it needed an alternative architecture that satisfied cost and quality at the same time.',
    decision: 'I moved from sole reliance on GPT-4V to a BLIP/CLIP/LLM 3-stage division of roles. BLIP (concrete-description captioning) + CLIP (Top-3 emotional-atmosphere classification) + LLM (natural-language narration) each handle only their specialized role. Removing the GPT-4V dependency reshaped the cost structure, and OpenVINO 4-bit quantization made BLIP/CLIP lightweight enough to run locally, improving response speed.',
    implementation: [
      { title: 'OpenVINO 4-bit Quantization', body: 'Applying OpenVINO 4-bit quantization and asyncio parallelism to BLIP and CLIP runs both simultaneously and merges the results. Response time dropped 30s to 20s with operating cost -30%, and CPU inference is possible without a GPU.' },
      { title: 'Django to FastAPI Migration', body: 'Django\'s synchronous ORM became a parallelization bottleneck, so the system was fully rebuilt on FastAPI + asyncio. Docker + AWS EC2 deployment secured operational stability and improved concurrent-request performance.' },
      { title: 'asyncio Parallelization & CLIP Emotion Classification', body: 'BLIP (concrete description) and CLIP (emotional atmosphere) run concurrently via asyncio.gather, shortening total time to max(BLIP, CLIP). CLIP quantifies the Top-3 atmospheres from the cosine similarity between 20 predefined emotion labels and the image embedding, raising the quality of the LLM narration.' },
      { title: 'Accessibility-first UX Design', body: 'A fully voice-driven loop — STT to AI narration to TTS — delivers an experience of "listening to" a photo without any screen or text. Feedback from testing with visually impaired users was incorporated, along with controls for narration length and speed.' },
    ],
    results: 'Reached the finals of the 2025 Korea Disability Hackathon. Achieved 30% lower operating cost versus GPT-4V and 30s to 20s response time, processed Stage 1 and 2 concurrently via BLIP+CLIP parallelism, and received positive feedback in testing with visually impaired users. Quantization improved speed but reduced BLIP\'s accuracy on complex image descriptions; replacing it with a stronger open-source VLM such as LLaVA and securing fine-tuning data are the key tasks for quality improvement.',
    stack: ['BLIP', 'CLIP', 'OpenVINO 4-bit', 'FastAPI', 'asyncio', 'Docker', 'AWS EC2', 'Kotlin'],
  },
  'pictag': {
    context: 'A SaaS that delivers real-time visitor Re-ID and movement heatmaps from CCTV footage on the low-spec edge devices (no GPU) found in small-business stores. There were two core challenges. (1) Embedding quality — how to extract Re-ID features from the YOLO backbone (Linear vs. Pooling vs. Attention). (2) Edge real-time performance — designing a structure that processes RTSP streaming and Re-ID inference simultaneously in a GPU-less environment.',
    decision: 'With no prior hypothesis, I A/B tested three embedding methods — Linear / Pooling / Attention — on identical data under identical conditions. Attention showed a 50%+ advantage in both Re-ID accuracy and training efficiency and was adopted. For edge real-time performance, Capture / Detection / Embedding / Re-ID were split into four independent threaded queues, and OpenVINO INT8 quantization secured inference speed.',
    implementation: [
      { title: 'Embedding A/B Experiment', body: 'After decomposing the YOLO backbone, three extraction heads — Linear / Pooling / Attention — were compared on identical data and training conditions. Attention showed a 50%+ advantage in both Re-ID accuracy and convergence speed and was adopted based on the experiment.' },
      { title: '4-Thread Independent Queue Design', body: 'The Capture / Detection / Embedding / Re-ID threads are connected by independent queues that buffer the differences in per-stage processing speed. Re-ID runs continuously with no camera-stream delay, and a thread failure restarts individually with no impact on the whole.' },
      { title: 'Edge Optimization & Dashboard', body: 'OpenVINO INT8 quantization achieved real-time edge inference without a GPU (4x smaller and 2-3x faster than FP32). Django + WebSocket renders visitor-movement heatmaps in real time, giving small businesses spatial operating insights.' },
    ],
    results: 'Attention embeddings delivered 50%+ Re-ID training efficiency, and the 4-thread pipeline enabled real-time Re-ID in a GPU-less edge environment. The Django + WebSocket heatmap dashboard validated the small-business SaaS business model. Re-ID accuracy is sensitive to changes in lighting and angle, so additional training on data from diverse camera environments is a task, and I plan to expand universal deployment to ARM-based edge devices via ONNX conversion.',
    stack: ['YOLOv8', 'Attention Embedding', 'OpenVINO INT8', 'RTSP', 'Python Threading', 'Django', 'WebSocket'],
  },
  'hosugator': {
    context: 'While deploying my personal portfolio (Next.js) on AWS, I faced two structural problems. (1) Cost: the ALB (~$20/month) + ECS Fargate (per-request billing) combination was excessive for a personal site with no traffic. (2) Security: storing IAM User access keys in GitHub Secrets is a long-lived-credential approach that risks unlimited permission theft if a key leaks.',
    decision: 'Next.js Static Export removed server dependencies, moving to S3 static hosting, and fully eliminating the IAM User in favor of OIDC Federation solved both problems structurally at once. GitHub Actions OIDC issues only short-lived tokens and grants least privilege (S3 + CloudFront only).',
    implementation: [
      { title: 'Architecture Shift', body: 'Moved from ALB + ECS Fargate ($20+/month) to S3 static hosting (under $1/month) and fully removed server dependencies via Next.js Static Export, achieving TCO -80%.' },
      { title: 'OIDC Federation', body: 'GitHub Actions OIDC issues only short-lived tokens and fully removes IAM User access keys. Least privilege (S3 + CloudFront only) structurally eliminates key-leak risk, and key rotation is no longer needed.' },
      { title: 'Troubleshooting: OIDC sub claim', body: 'A misunderstanding of the GitHub OIDC token\'s sub claim format caused the IAM Role trust policy to fail repeatedly. Reading the official AWS docs and decoding the actual token yielded the correct condition expression.' },
      { title: 'k3s Migration Experiment & Deploy Strategy', body: 'I experimented with moving from serverless to self-managed EC2/Nginx/k3s, experiencing the cost-vs-complexity trade-off firsthand before settling on static hosting. GitHub Actions uploads to S3 and then triggers a CloudFront invalidation automatically, ensuring zero downtime during deploys.' },
    ],
    results: 'Achieved TCO -80% ($20+/month to under $1) and complete IAM User removal (zero IAM Users in a security audit). Short-lived OIDC tokens greatly raised the security level, and CloudFront CDN global delivery plus CI/CD automation were completed. I confirmed firsthand — through a direct cost comparison — that the essence of infrastructure optimization is "only what you need," that serverless is not always the answer, and that security too is about structural minimization rather than complex policy.',
    stack: ['Next.js Static', 'AWS S3', 'CloudFront', 'Route53', 'IAM OIDC', 'k3s', 'GitHub Actions'],
  },
  'cureat': {
    context: 'A service that delivers personalized restaurant recommendations from fragmented food data (blogs, SNS, news, RSS). There were three core problems. (1) Data quality — advertorial and promotional content polluting search results. (2) Collection efficiency — the bottleneck of synchronous multi-source collection made real-time impossible. (3) Recommendation accuracy — the semantic limits of simple keyword search under-reflected individual taste. Governance of clean food data was the core of service trust.',
    decision: 'Ko-BERT detects advertorial content at the contextual level, using vector similarity to filter even natural-sounding ad sentences that rules alone miss. asyncio parallel collection gathers three sources — REST API + web crawling + RSS — simultaneously, removing the I/O bottleneck. A 2-stage hybrid search (ChromaDB cosine similarity + location-distance weighting) improved personalized-recommendation accuracy.',
    implementation: [
      { title: 'Data Collection Pipeline', body: 'Three sources — REST, Crawl, RSS — are collected in parallel via asyncio.gather, and duplicate URLs are filtered with SHA-256 hashing. A real-time refresh scheduler keeps food data current and fully removes the I/O bottleneck.' },
      { title: 'Ko-BERT 2-Stage Filtering', body: 'Stage 1 is a rule filter based on an ad-keyword dictionary; Stage 2 detects natural-sounding ad sentences via Ko-BERT contextual-embedding cosine similarity. Complementing rules with vectors removed 20%+ more ads.' },
      { title: '2-Stage Hybrid Search', body: 'Stage 1 extracts the Top-K semantically similar restaurants via ChromaDB cosine similarity, and Stage 2 re-ranks by spatial context using the user\'s current-location distance weighting. Combining meaning and location improved personalized-recommendation accuracy.' },
      { title: 'Team Collaboration & Code-Quality Management', body: 'Two-week Jira sprints developed the four components — collection, filtering, search, app — separately, and on GitLab we introduced a no-self-approval policy and mandatory review of async logic. Auto-generated OpenAPI and ADR management of crawl-target change history levelled up team code quality.' },
    ],
    results: 'Removing 20%+ of advertorial content improved data quality, and asyncio parallel collection removed the multi-source I/O bottleneck. The 2-stage hybrid search improved recommendation accuracy over keywords, and integration with a React Native mobile app was completed. Tuning the Ko-BERT filter threshold to optimize the precision-recall balance, and adding history-based personalization (User-Based CF), remain open tasks.',
    stack: ['Ko-BERT', 'ChromaDB', 'FastAPI', 'asyncio', 'React Native', 'PostgreSQL', 'Jira', 'GitLab'],
  },
  'dorosee': {
    context: 'A UGV (unmanned ground vehicle) platform that lets older adults and people with disabilities get AI help in an urban emergency without operating a smartphone. The 24-hour hackathon constraint meant AI-module development and integration had to be completed without actual UGV hardware. Moreover, a single-trigger approach (vision only or voice only) carried a high risk of misfiring, which could cause a critical reliability problem for the UX of underserved groups.',
    decision: 'I implemented a Unity 3D HAL (Hardware Abstraction Layer) in simulation to complete AI-module integration testing without a real UGV. A triple-trigger design composed vision (YOLOv8) + motion (MediaPipe greeting gesture) + voice (STT wakeword) as independent triggers, and a condition of 2-of-3 simultaneous detection sustained for 10 seconds structurally minimized misfiring.',
    implementation: [
      { title: 'YOLOv8 Fine-tuning', body: 'Fine-tuned emergency detection on a 3,000-image dataset, and a 10-second sustained-detection condition structurally minimized momentary false positives. Achieved Recall 92% / Precision 85%, including data from underserved-group environments.' },
      { title: 'Triple-Trigger Fusion Decision', body: 'The independent vision + motion + voice triggers are fused by weighted voting, greatly reducing misfiring versus a single trigger. It covers the diverse ways digitally underserved groups (older adults, people with disabilities) express themselves.' },
      { title: 'Unity 3D HAL Strategy', body: 'Implementing the Hardware Abstraction Layer as a Unity 3D simulation completed integration testing of the entire AI pipeline within 24 hours without a real UGV. It demonstrated a "finish the software, then mount the hardware" paradigm.' },
    ],
    results: 'Won the grand prize at the 2025 UWC hackathon. Secured YOLOv8 Recall 92% / Precision 85%, validated the underserved-group UX with a fully voice-driven STT to LLM to TTS loop, and completed automatic AED-report integration. The Unity HAL demonstrated a "software first, hardware later" design philosophy; ROS integration and triple-trigger weight optimization for an actual UGV are the next tasks.',
    stack: ['YOLOv8', 'MediaPipe', 'STT/TTS', 'LLM API', 'Unity 3D HAL', 'FastAPI', 'PyTorch'],
  },
  'kdlc': {
    context: 'A product-demand forecasting competition based on distribution-center logistics data. There were three core challenges. (1) Time-series complexity — a multi-layered pattern mixing periodicity (weekly, monthly, yearly), trend, and noise. (2) Data leakage — the fatal error of future data entering training when random K-Fold is applied. (3) Single-model limits — no single model captures seasonality, long-range dependency, and nonlinear feature importance at once. Before model selection, feature design and validation structure decided the outcome.',
    decision: 'I prioritized feature engineering. Over 45 features — including Lag, Rolling, periodicity encoding, and domain-knowledge features — were the core driver of the performance gain. TimeSeriesSplit applied order-preserving cross-validation to structurally block data leakage, and a 3-model ensemble of SARIMA (seasonality) + LSTM (long-range dependency) + LightGBM (nonlinear features) let each model complement the others\' strengths.',
    implementation: [
      { title: '45+ Feature Engineering Strategy', body: 'Designed Lag (1-7 days), Rolling statistics (7/14/30-day mean, variance, max), sin/cos periodicity encoding, holiday binaries, and promotion flags. Domain-knowledge features contributed most to the performance gain, and feature-importance analysis removed unnecessary features.' },
      { title: '3-Model Ensemble Design', body: 'SARIMA does weekly/monthly seasonal ARIMA decomposition, LSTM learns long-range dependency over 30-day sequences, and LightGBM captures the nonlinear importance of 45+ features. Ensembling with weights inversely proportional to each model\'s validation RMSE reduced prediction variance versus any single model.' },
      { title: 'TimeSeriesSplit Cross-Validation', body: 'Banning random K-Fold in favor of order-preserving splits (past Train to future Val) structurally blocked the data leakage of future data entering training. Matching the validation conditions to the actual production environment raised the reliability of overfitting detection.' },
      { title: 'sin/cos Periodicity & Weighted-Ensemble Optimization', body: 'Transforming day-of-week and month via sin/cos gives a continuous cyclic representation, preserving periodicity with no discontinuity at the December-to-January boundary. Normalizing the inverse of each model\'s TimeSeriesSplit validation RMSE to derive weights further improved the final RMSE over a simple-average ensemble.' },
    ],
    results: 'Confirmed that feature engineering affects forecasting performance more than model selection. After applying TimeSeriesSplit, the gap between validation metrics and actual prediction error dropped sharply, and the ensemble improved RMSE over any single model. Automating SARIMA parameter grid search, LSTM hyperparameter optimization (Optuna), and adding external data (weather, economic indicators) are the improvement tasks.',
    stack: ['SARIMA', 'LSTM', 'LightGBM', 'TimeSeriesSplit', 'pandas', 'scikit-learn', 'PyTorch'],
  },
  'go2fit': {
    context: 'An Android-first fitness app combining workout logging with community social features, built full-stack from a FastAPI backend through an AI pose-analysis pipeline, cloud infrastructure, and a Flutter app. It had to solve real operational constraints simultaneously: the unstable network environment of gyms, Kakao-based authentication security, and video AI analysis on low-cost, GPU-less cloud.',
    decision: 'I built it around Kakao OAuth-based JWT dual-token (Rotation) authentication, MediaPipe 33-landmark rule-based pose analysis, and an offline sync queue (Idempotency + Bulk). For cloud, after a provider cost comparison I chose Oracle x86 Paid ($27/month), deploying with Docker multi-stage + GitLab CI/CD + k3s. The client was structured with Flutter Feature-based Clean Architecture.',
    implementation: [
      { title: 'Data Model & Kakao OAuth JWT', body: 'Designed 10 tables — 5 workout-log tables in a users to session to exercise to set to rep_analysis hierarchy, 3 for authentication, and 3 for community. After Kakao server verification, Access (15 min) / Refresh (30 day) JWTs are issued; raw tokens live only on the client while the DB stores only SHA-256 hashes, and on detecting Refresh reuse all of that user\'s tokens are immediately blacklisted. Hypothesis property-based testing automatically validates the boundary cases of token forgery, expiry, and reuse.' },
      { title: 'MediaPipe AI Pose-Analysis Pipeline', body: 'On video upload it returns 202 immediately and processes asynchronously: OpenCV frame extraction to MediaPipe Pose 33 landmarks to ExerciseAnalyzer (a domain layer with no DB or HTTP dependency) to per-rep score 0-100 and feedback[] to DB save. BenchPress, Squat, and Deadlift are modularized independently as rule-based modules, so a new exercise is added by registering one line in the SUPPORTED_EXERCISES dict.' },
      { title: 'Offline Sync Queue + Bulk API', body: 'To cope with unstable gym networks, an X-Idempotency-Key middleware returns the stored response without re-executing on duplicate requests (Cache HIT). The Bulk API processes up to 100 items with All-or-Nothing atomicity — one failure rolls back the whole batch — and SQLAlchemy bulk_insert_mappings minimizes DB load.' },
      { title: 'Oracle Cloud & Flutter PosePainter', body: 'Chose Oracle x86 Paid at $27/month (vs. AWS $61, Azure $70-80), using OCIR 5GB free and the Chuncheon region, served over the go2fits.com domain and DNS secured via Cloudflare. Deployment uses Docker multi-stage and GitLab CI/CD (k3s ctr import to rollout restart to alembic upgrade to auto-numbered git tag). Flutter renders the landmarks returned by the server (Float32List, 33 points) over the VideoPlayer via a CustomPainter (PosePainter), preventing GC thrashing with Paint-object caching and a visibility filter, and stores the JWT in flutter_secure_storage (Android Keystore).' },
    ],
    results: 'Shipped a FastAPI + SQLAlchemy + PostgreSQL backend and a Flutter app (Android-first) on the Google Play Store (backend from 2025.11, app from 2026). It serves over HTTPS on the go2fits.com domain and DNS secured via Cloudflare, and the Oracle x86 $27/month infrastructure achieved under half the cost of AWS or Azure. Kakao OAuth JWT Rotation security, MediaPipe (33 landmarks) rule-based 3-exercise pose analysis, and the offline sync queue were all validated in a real production environment.',
    stack: ['FastAPI', 'SQLAlchemy', 'PostgreSQL', 'Alembic', 'Pydantic v2', 'Kakao OAuth', 'JWT', 'Hypothesis', 'MediaPipe', 'OpenCV', 'Cloudflare', 'Docker multi-stage', 'k3s', 'Oracle Cloud', 'GitLab CI/CD', 'Flutter', 'Android', 'CustomPainter', 'flutter_secure_storage'],
  },
};
